# HN-INFRA-009 — local/CI test dependencies (not required to run the API).
# Install: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0,<10
pytest-asyncio>=0.23.0,<2
anyio>=4.0.0,<5
